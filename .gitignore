# ── Game ROM & anything derived from it (COPYRIGHT — never commit) ────────────
*.nds
/dsd_extract/
/extract/
/asm/
/build/*.json
/build/progress_audit.md
/build/objects.txt
/build/file_modes.json
/build/*.xMAP
/build/*.sav
!/build/known_mismatches.txt

# ── Emulator trace-coverage tooling (local-only, regenerable) ─────────────────
/emu_trace/

# ── Build artifacts (regenerated locally) ─────────────────────────────────────
/build/delinks/
/build/obj/
/build/_test/
/build/build/
/build/link/
/build/src/
/build.ninja
/.ninja_deps
/.ninja_log
/objdiff.json
*.o
*.elf
*.lcf
*.bin

# ── Toolchain — each contributor provides their own (see CONTRIBUTING.md) ─────
/tools/mwccarm/
/tools/dsd.exe
/tools/wibo
/tools/ds-decomp-src/
/tools/_cw/
*.exe
*.dll

# ── Local / one-off setup scripts & notes ─────────────────────────────────────
/SETUP_MWCCARM.md
/recon.py
/decompress.py
/diffcheck.py
/scan_overlays.py
/src/test.c

# ── Local staging / scratch (not committed) ───────────────────────────────────
/staging/

# ── Misc ──────────────────────────────────────────────────────────────────────
__pycache__/
*.pyc
*.png
*.jpg
*.zip
/pc_theater/
.DS_Store

# ── SDK identification harness ────────────────────────────────────────────────
# Third-party SDK source (pokediamond NitroSDK decomp) — re-stage locally, not redistributed
/scratch/
/sdk/src/
/sdk/build/*
!/sdk/build/names.json
# pokediamond headers (third-party) — only commit the small stubs WE wrote
/sdk/include/*.h
/sdk/include/nitro/
!/sdk/include/code32.h
!/sdk/include/code16.h
!/sdk/include/function_target.h
!/sdk/include/main.h
!/sdk/include/mmap.h

# Local-only agent configuration and decomp skills (not redistributed)
/.claude/

# ── Ghidra project (ROM-derived disassembly DB — never commit) ───────────────
*.gpr
*.rep/
/ghidra_*/
.ghidra/

# Scratch del bucle de decomp: candidatos .c antes de verificarlos y barridos intermedios.
# NO deben subirse -- son locales y efimeros (se llegaron a colar 3.393 ficheros el 2026-07-19).
/build/try/
# Standing rule: build/ is never uploaded. Ignore the whole tree and keep the two
# tracked files as explicit exceptions.
/build/*
!/build/known_mismatches.txt
!/build/decompiled.txt
/build/sweep/
/build/dme/
/build/poolexp/

# Scratch de renombrados (local, regenerable)
/build/_switch_rename.txt

# Notas de trabajo internas para el usuario -- no van al repo publico
/dudas.md

# khdays active-function mechanical state
build/active_function.json
build/semantic_receipts/
build/match_receipts/
build/completed_functions/

# Agent working files: sweep scripts, receipts and diff helpers.
scratchpad/
